{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the Taproot POS desktop window. The window loads the remote taproot-pos.com origin, so `remote.urls` grants that origin access to the invoke bridge (native printing / cash drawer).",
  "windows": ["main"],
  "remote": {
    "urls": ["https://taproot-pos.com", "https://*.taproot-pos.com"]
  },
  "permissions": [
    "core:default",
    "shell:allow-open",
    "notification:default"
  ]
}
