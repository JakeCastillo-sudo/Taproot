[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "npm run build --workspace=@taproot/shared",
  "cd apps/api && npx tsc"
]

[start]
cmd = "node apps/api/dist/index.js"
